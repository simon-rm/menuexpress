# config/database.yml

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: medb_dev

test:
  <<: *default
  database: medb_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
